name: ej_stepper
description: Stepper with a brand new design. Easy to use with similar properties to material stepper.
version: 0.1.1
homepage: https://github.com/EhsnJmli/ej_stepper.git

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
